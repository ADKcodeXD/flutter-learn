import 'dart:ui';

import 'package:flutter/cupertino.dart';

class AppRadius {
  static const Radius r10px = Radius.circular(10);
  static const Radius r5px = Radius.circular(5);
  static BorderRadius br10px = BorderRadius.circular(10);
  static BorderRadius br5px = BorderRadius.circular(5);
}
